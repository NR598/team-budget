<%= form_with model: [ @transactions, @transactions.notes.build ] do |form| %>
  <p>
    <%= form.label :note %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>